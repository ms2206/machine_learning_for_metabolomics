PLOTS_DIR <- file.path(OUTPUT_DIR, "plots")
date_str <- format(Sys.Date(), "%Y-%m-%d")
# source utility functions
source(UTILS_FILEPATH)
print(paste("Utility functions sourced from:", UTILS_FILEPATH))
# load DCA two
dca_y2 <- load_data(file.path(DATA_DIR, "DCA_Year2.csv"))
# infer mean values for missing data in dca_y2
dca_y2 <- infer_mean_for_nan(dca_y2)
# set Sample.ID as row names and remove from columns
rownames(dca_y2) <- dca_y2$Sample.ID
dca_y2$Sample.ID <- NULL
dca_y2$storage_condition <- "DCA"
# load CA two
ca_y2 <- load_data(file.path(DATA_DIR, "CA_Year2.csv"))
rownames(ca_y2) <- ca_y2$Sample.ID
ca_y2$Sample.ID <- NULL
ca_y2$storage_condition <- "CA"
# merge DCA and CA data for Year 2
combined_y1 <- rbind(dca_y2, ca_y2)
View(combined_y1)
View(combined_y1)
str(combined_y1)
ls("package:Rcatch22")
table(combined_y1$storage_condition, combined_y1$Storage.Week)
# order Storage.Week as a factor with correct levels
combined_y1$Storage.Week <- factor(
combined_y1$Storage.Week,
levels = c("0, 5, 11, 17, 23, 29)") # ensure correct order
)
View(combined_y1)
View(combined_y1)
# load libraries
library(ggplot2)
library(caret)
library(dotenv)
library(dplyr)
library(Rcatch22)
cat("Loading environment variables from .env file...\n")
tryCatch(
{
dotenv::load_dot_env()
cat("Environment variables loaded successfully.\n")
},
error = function(e) {
cat("Error loading environment variables:", e$message, "\n")
cat("Loading from hardcoded path as fallback...\n")
dotenv::load_dot_env("/Users/mspriggs/Library/CloudStorage/OneDrive-Illumina,Inc./Documents/Applied_Bioinformatics/modules/machine_learning_for_metabolomics/assignment/scripts/.env") # nolint
cat("Loaded .env from hardcoded path\n")
}
)
cat("Setting up global variables and sourcing utility functions...\n")
WORKING_DIR <- Sys.getenv("WORKING_DIR")
# define file paths
UTILS_FILEPATH <- file.path(WORKING_DIR, "scripts", "00_data_prep_utils.R")
DATA_DIR <- file.path(WORKING_DIR, "Assignment_Data")
OUTPUT_DIR <- file.path(WORKING_DIR, "outputs")
PLOTS_DIR <- file.path(OUTPUT_DIR, "plots")
date_str <- format(Sys.Date(), "%Y-%m-%d")
# source utility functions
source(UTILS_FILEPATH)
print(paste("Utility functions sourced from:", UTILS_FILEPATH))
# load DCA two
dca_y2 <- load_data(file.path(DATA_DIR, "DCA_Year2.csv"))
# infer mean values for missing data in dca_y2
dca_y2 <- infer_mean_for_nan(dca_y2)
# set Sample.ID as row names and remove from columns
rownames(dca_y2) <- dca_y2$Sample.ID
dca_y2$Sample.ID <- NULL
dca_y2$storage_condition <- "DCA"
# load CA two
ca_y2 <- load_data(file.path(DATA_DIR, "CA_Year2.csv"))
rownames(ca_y2) <- ca_y2$Sample.ID
ca_y2$Sample.ID <- NULL
ca_y2$storage_condition <- "CA"
# merge DCA and CA data for Year 2
combined_y1 <- rbind(dca_y2, ca_y2)
table(combined_y1$storage_condition, combined_y1$Storage.Week)
# order Storage.Week as a factor with correct levels
combined_y1$Storage.Week <- factor(
combined_y1$Storage.Week,
levels = c("0, 5, 11, 17, 23, 29")
ordered = TRUE
# order Storage.Week as a factor with correct levels
combined_y1$Storage.Week <- factor(
combined_y1$Storage.Week,
levels = c("0, 5, 11, 17, 23, 29"),
ordered = TRUE
)
# load libraries
library(ggplot2)
library(caret)
library(dotenv)
library(dplyr)
library(Rcatch22)
cat("Loading environment variables from .env file...\n")
tryCatch(
{
dotenv::load_dot_env()
cat("Environment variables loaded successfully.\n")
},
error = function(e) {
cat("Error loading environment variables:", e$message, "\n")
cat("Loading from hardcoded path as fallback...\n")
dotenv::load_dot_env("/Users/mspriggs/Library/CloudStorage/OneDrive-Illumina,Inc./Documents/Applied_Bioinformatics/modules/machine_learning_for_metabolomics/assignment/scripts/.env") # nolint
cat("Loaded .env from hardcoded path\n")
}
)
cat("Setting up global variables and sourcing utility functions...\n")
WORKING_DIR <- Sys.getenv("WORKING_DIR")
# define file paths
UTILS_FILEPATH <- file.path(WORKING_DIR, "scripts", "00_data_prep_utils.R")
DATA_DIR <- file.path(WORKING_DIR, "Assignment_Data")
OUTPUT_DIR <- file.path(WORKING_DIR, "outputs")
PLOTS_DIR <- file.path(OUTPUT_DIR, "plots")
date_str <- format(Sys.Date(), "%Y-%m-%d")
# source utility functions
source(UTILS_FILEPATH)
print(paste("Utility functions sourced from:", UTILS_FILEPATH))
# load DCA two
dca_y2 <- load_data(file.path(DATA_DIR, "DCA_Year2.csv"))
# infer mean values for missing data in dca_y2
dca_y2 <- infer_mean_for_nan(dca_y2)
# set Sample.ID as row names and remove from columns
rownames(dca_y2) <- dca_y2$Sample.ID
dca_y2$Sample.ID <- NULL
dca_y2$storage_condition <- "DCA"
# load CA two
ca_y2 <- load_data(file.path(DATA_DIR, "CA_Year2.csv"))
rownames(ca_y2) <- ca_y2$Sample.ID
ca_y2$Sample.ID <- NULL
ca_y2$storage_condition <- "CA"
# merge DCA and CA data for Year 2
combined_y1 <- rbind(dca_y2, ca_y2)
combined_y1$Storage.Week <- factor(
combined_y1$Storage.Week,
levels  = c(0, 5, 11, 17, 23, 29), # numeric vector, not a single string
ordered = TRUE
)
# load libraries
library(ggplot2)
library(caret)
library(dotenv)
library(dplyr)
library(Rcatch22)
cat("Loading environment variables from .env file...\n")
tryCatch(
{
dotenv::load_dot_env()
cat("Environment variables loaded successfully.\n")
},
error = function(e) {
cat("Error loading environment variables:", e$message, "\n")
cat("Loading from hardcoded path as fallback...\n")
dotenv::load_dot_env("/Users/mspriggs/Library/CloudStorage/OneDrive-Illumina,Inc./Documents/Applied_Bioinformatics/modules/machine_learning_for_metabolomics/assignment/scripts/.env") # nolint
cat("Loaded .env from hardcoded path\n")
}
)
cat("Setting up global variables and sourcing utility functions...\n")
WORKING_DIR <- Sys.getenv("WORKING_DIR")
# define file paths
UTILS_FILEPATH <- file.path(WORKING_DIR, "scripts", "00_data_prep_utils.R")
DATA_DIR <- file.path(WORKING_DIR, "Assignment_Data")
OUTPUT_DIR <- file.path(WORKING_DIR, "outputs")
PLOTS_DIR <- file.path(OUTPUT_DIR, "plots")
date_str <- format(Sys.Date(), "%Y-%m-%d")
# source utility functions
source(UTILS_FILEPATH)
print(paste("Utility functions sourced from:", UTILS_FILEPATH))
# load DCA two
dca_y2 <- load_data(file.path(DATA_DIR, "DCA_Year2.csv"))
# infer mean values for missing data in dca_y2
dca_y2 <- infer_mean_for_nan(dca_y2)
# set Sample.ID as row names and remove from columns
rownames(dca_y2) <- dca_y2$Sample.ID
dca_y2$Sample.ID <- NULL
dca_y2$storage_condition <- "DCA"
# load CA two
ca_y2 <- load_data(file.path(DATA_DIR, "CA_Year2.csv"))
rownames(ca_y2) <- ca_y2$Sample.ID
ca_y2$Sample.ID <- NULL
ca_y2$storage_condition <- "CA"
# merge DCA and CA data for Year 2
combined_y1 <- rbind(dca_y2, ca_y2)
table(combined_y1$storage_condition, combined_y1$Storage.Week)
combined_y1$Storage.Week <- factor(
combined_y1$Storage.Week,
levels = c(0, 5, 11, 17, 23, 29), # numeric vector, not a single string
)
# order by Storage.Week
combined_y1 <- combined_y1[order(combined_y1$Storage.Week), ]
# exclude Storage.Week, Ripening.Stage, and storage_condition from features
features <- combined_y1 %>%
select(-Storage.Week, -Ripening.Stage, -storage_condition)
# exclude Storage.Week, Ripening.Stage, and storage_condition from features
features <- combined_y1 %>%
dplyr::select(-Storage.Week, -Ripening.Stage, -storage_condition)
View(features)
View(features)
all(sapply(features, is.numeric)) == TRUE
sum(is.na(features))
dat <- bind_cols(
combined_y2 %>% select(storage_condition),
features
)
View(combined_y1)
# load libraries
library(ggplot2)
library(caret)
library(dotenv)
library(dplyr)
library(Rcatch22)
cat("Loading environment variables from .env file...\n")
tryCatch(
{
dotenv::load_dot_env()
cat("Environment variables loaded successfully.\n")
},
error = function(e) {
cat("Error loading environment variables:", e$message, "\n")
cat("Loading from hardcoded path as fallback...\n")
dotenv::load_dot_env("/Users/mspriggs/Library/CloudStorage/OneDrive-Illumina,Inc./Documents/Applied_Bioinformatics/modules/machine_learning_for_metabolomics/assignment/scripts/.env") # nolint
cat("Loaded .env from hardcoded path\n")
}
)
cat("Setting up global variables and sourcing utility functions...\n")
WORKING_DIR <- Sys.getenv("WORKING_DIR")
# define file paths
UTILS_FILEPATH <- file.path(WORKING_DIR, "scripts", "00_data_prep_utils.R")
DATA_DIR <- file.path(WORKING_DIR, "Assignment_Data")
OUTPUT_DIR <- file.path(WORKING_DIR, "outputs")
PLOTS_DIR <- file.path(OUTPUT_DIR, "plots")
date_str <- format(Sys.Date(), "%Y-%m-%d")
# source utility functions
source(UTILS_FILEPATH)
print(paste("Utility functions sourced from:", UTILS_FILEPATH))
# load DCA two
dca_y2 <- load_data(file.path(DATA_DIR, "DCA_Year2.csv"))
# infer mean values for missing data in dca_y2
dca_y2 <- infer_mean_for_nan(dca_y2)
# set Sample.ID as row names and remove from columns
rownames(dca_y2) <- dca_y2$Sample.ID
dca_y2$Sample.ID <- NULL
dca_y2$storage_condition <- "DCA"
# load CA two
ca_y2 <- load_data(file.path(DATA_DIR, "CA_Year2.csv"))
rownames(ca_y2) <- ca_y2$Sample.ID
ca_y2$Sample.ID <- NULL
ca_y2$storage_condition <- "CA"
# merge DCA and CA data for Year 2
combined_y2 <- rbind(dca_y2, ca_y2)
table(combined_y2$storage_condition, combined_y2$Storage.Week)
combined_y2$Storage.Week <- factor(
combined_y2$Storage.Week,
levels = c(0, 5, 11, 17, 23, 29), # numeric vector, not a single string
)
# order by Storage.Week
combined_y2 <- combined_y2[order(combined_y2$Storage.Week), ]
# exclude Storage.Week, Ripening.Stage, and storage_condition from features
features <- combined_y2 %>%
dplyr::select(-Storage.Week, -Ripening.Stage, -storage_condition)
# exclude Storage.Week, Ripening.Stage, and storage_condition from features
features <- combined_y2 %>%
dplyr::select(-Storage.Week, -Ripening.Stage, -storage_condition)
dat <- bind_cols(
combined_y2 %>% select(storage_condition),
features
)
combined_y2 %>% select(storage_condition)
colnames(combined_y2)
combined_y2 %>% select(storage_condition)
combined_y2 %>% dyplr::select(storage_condition)
combined_y2 %>% dplyr::select(storage_condition)
dat <- bind_cols(
combined_y2 %>% dplyr::select(storage_condition),
features
)
dat
View(dat)
sep_tbl <- dat %>%
group_by(storage_condition) %>%
summarise(across(where(is.numeric), ~ mean(.x, na.rm = TRUE)), .groups = "drop") %>%
tidyr::pivot_longer(-storage_condition, names_to = "variable", values_to = "mean_value") %>%
tidyr::pivot_wider(names_from = storage_condition, values_from = mean_value) %>%
mutate(abs_mean_diff = abs(CA - DCA)) %>%
arrange(desc(abs_mean_diff))
View(sep_tbl)
topN <- 20
top_vars <- sep_tbl %>% slice_head(n = topN) %>% pull(variable)
print(sep_tbl, n = 30)
top_vars
ncols(features)
length(colnames(features))
names(features)
names(combined_y2)[1:5]
("storage_condition" %in% names(combined_y2))
all(names(features) %in% names(combined_y2))
mat
# load libraries
library(ggplot2)
library(caret)
library(dotenv)
library(dplyr)
library(Rcatch22)
cat("Loading environment variables from .env file...\n")
tryCatch(
{
dotenv::load_dot_env()
cat("Environment variables loaded successfully.\n")
},
error = function(e) {
cat("Error loading environment variables:", e$message, "\n")
cat("Loading from hardcoded path as fallback...\n")
dotenv::load_dot_env("/Users/mspriggs/Library/CloudStorage/OneDrive-Illumina,Inc./Documents/Applied_Bioinformatics/modules/machine_learning_for_metabolomics/assignment/scripts/.env") # nolint
cat("Loaded .env from hardcoded path\n")
}
)
cat("Setting up global variables and sourcing utility functions...\n")
WORKING_DIR <- Sys.getenv("WORKING_DIR")
# define file paths
UTILS_FILEPATH <- file.path(WORKING_DIR, "scripts", "00_data_prep_utils.R")
DATA_DIR <- file.path(WORKING_DIR, "Assignment_Data")
OUTPUT_DIR <- file.path(WORKING_DIR, "outputs")
PLOTS_DIR <- file.path(OUTPUT_DIR, "plots")
date_str <- format(Sys.Date(), "%Y-%m-%d")
# source utility functions
source(UTILS_FILEPATH)
print(paste("Utility functions sourced from:", UTILS_FILEPATH))
# load DCA two
dca_y2 <- load_data(file.path(DATA_DIR, "DCA_Year2.csv"))
# infer mean values for missing data in dca_y2
dca_y2 <- infer_mean_for_nan(dca_y2)
# set Sample.ID as row names and remove from columns
rownames(dca_y2) <- dca_y2$Sample.ID
dca_y2$Sample.ID <- NULL
dca_y2$storage_condition <- "DCA"
# load CA two
ca_y2 <- load_data(file.path(DATA_DIR, "CA_Year2.csv"))
rownames(ca_y2) <- ca_y2$Sample.ID
ca_y2$Sample.ID <- NULL
ca_y2$storage_condition <- "CA"
# merge DCA and CA data for Year 2
combined_y2 <- rbind(dca_y2, ca_y2)
table(combined_y2$storage_condition, combined_y2$Storage.Week)
weeks <- c(0, 5, 11, 17, 23, 29)
combined_y2$Storage.Week <- factor(
combined_y2$Storage.Week,
levels = weeks, # numeric vector, not a single string
)
# order by Storage.Week
combined_y2 <- combined_y2[order(combined_y2$Storage.Week), ]
# exclude Storage.Week, Ripening.Stage, and storage_condition from features
features <- combined_y2 %>%
dplyr::select(-Storage.Week, -Ripening.Stage, -storage_condition)
vars <- colnames(features)
n0 <- 12 # minimum number of samples per class (DCA and CA) for each storage week
mat <- do.call(
rbind,
lapply(weeks, function(w) {
df_w <- combined_y2 %>%
filter(storage_condition == "CA", Storage.Week == w) %>%
dyplr::select(all_of(vars))
idx <- sample.int(nrow(df_w),
size = n0,
replace = TRUE
)
})
)
vars <- colnames(features)
n0 <- 12 # minimum number of samples per class (DCA and CA) for each storage week
mat <- do.call(
rbind,
lapply(weeks, function(w) {
df_w <- combined_y2 %>%
filter(storage_condition == "CA", Storage.Week == w) %>%
dplyr::select(all_of(vars))
idx <- sample.int(nrow(df_w),
size = n0,
replace = TRUE
)
})
)
rownames(mat) <- weeks
colnames(mat) <- vars
set.seed(1)
vars  <- names(features)
n0    <- 12
mat <- do.call(rbind, lapply(weeks, function(w) {
df_w <- combined_y2 %>%
filter(storage_condition == "CA", Storage.Week == w) %>%
select(all_of(vars))
idx <- sample.int(nrow(df_w), size = n0, replace = TRUE)
colMeans(df_w[idx, , drop = FALSE], na.rm = TRUE)
}))
set.seed(1)
weeks <- c(0, 5, 11, 17, 23, 29)
vars <- names(features)
n0 <- 12
mat <- do.call(rbind, lapply(weeks, function(w) {
df_w <- combined_y2 %>%
filter(storage_condition == "CA", Storage.Week == w) %>%
select(all_of(vars))
idx <- sample.int(nrow(df_w), size = n0, replace = TRUE)
colMeans(df_w[idx, , drop = FALSE], na.rm = TRUE)
}))
set.seed(1)
weeks <- c(0, 5, 11, 17, 23, 29)
vars <- names(features)
n0 <- 12
mat <- do.call(rbind, lapply(weeks, function(w) {
df_w <- combined_y2 %>%
filter(storage_condition == "CA", Storage.Week == w) %>%
dplyr::select(all_of(vars))
idx <- sample.int(nrow(df_w), size = n0, replace = TRUE)
colMeans(df_w[idx, , drop = FALSE], na.rm = TRUE)
}))
rownames(mat) <- weeks
colnames(mat) <- vars
View(mat)
View(mat)
# sanity checks
dim(mat) # should be 6 36
mat[1:2, 1:6] # preview
is.numeric(mat) # TRUE
rownames(mat)
colnames(mat)[1:10]
colnames(mat)
# try to run catch22 on one feature to test
f_firm <- catch22_all(mat[, "Firmness"])
length(f_firm)
str(f_firm)
length(f_firm$names)
names(f_firm)
f_firm$names
names(f_firm)
sapply(f_firm, function(z) paste(class(z), length(z)))
# load libraries
library(ggplot2)
library(caret)
library(dotenv)
library(dplyr)
library(Rcatch22)
cat("Loading environment variables from .env file...\n")
tryCatch(
{
dotenv::load_dot_env()
cat("Environment variables loaded successfully.\n")
},
error = function(e) {
cat("Error loading environment variables:", e$message, "\n")
cat("Loading from hardcoded path as fallback...\n")
dotenv::load_dot_env("/Users/mspriggs/Library/CloudStorage/OneDrive-Illumina,Inc./Documents/Applied_Bioinformatics/modules/machine_learning_for_metabolomics/assignment/scripts/.env") # nolint
cat("Loaded .env from hardcoded path\n")
}
)
cat("Setting up global variables and sourcing utility functions...\n")
WORKING_DIR <- Sys.getenv("WORKING_DIR")
# define file paths
UTILS_FILEPATH <- file.path(WORKING_DIR, "scripts", "00_data_prep_utils.R")
DATA_DIR <- file.path(WORKING_DIR, "Assignment_Data")
OUTPUT_DIR <- file.path(WORKING_DIR, "outputs")
PLOTS_DIR <- file.path(OUTPUT_DIR, "plots")
date_str <- format(Sys.Date(), "%Y-%m-%d")
# source utility functions
source(UTILS_FILEPATH)
print(paste("Utility functions sourced from:", UTILS_FILEPATH))
# load DCA two
dca_y2 <- load_data(file.path(DATA_DIR, "DCA_Year2.csv"))
# infer mean values for missing data in dca_y2
dca_y2 <- infer_mean_for_nan(dca_y2)
# set Sample.ID as row names and remove from columns
rownames(dca_y2) <- dca_y2$Sample.ID
dca_y2$Sample.ID <- NULL
dca_y2$storage_condition <- "DCA"
# load CA two
ca_y2 <- load_data(file.path(DATA_DIR, "CA_Year2.csv"))
rownames(ca_y2) <- ca_y2$Sample.ID
ca_y2$Sample.ID <- NULL
ca_y2$storage_condition <- "CA"
# merge DCA and CA data for Year 2
combined_y2 <- rbind(dca_y2, ca_y2)
table(combined_y2$storage_condition, combined_y2$Storage.Week)
weeks <- c(0, 5, 11, 17, 23, 29)
combined_y2$Storage.Week <- factor(
combined_y2$Storage.Week,
levels = weeks, # numeric vector, not a single string
)
# order by Storage.Week
combined_y2 <- combined_y2[order(combined_y2$Storage.Week), ]
# exclude Storage.Week, Ripening.Stage, and storage_condition from features
features <- combined_y2 %>%
dplyr::select(-Storage.Week, -Ripening.Stage, -storage_condition)
set.seed(1)
weeks <- c(0, 5, 11, 17, 23, 29)
vars <- names(features)
n0 <- 12
mat <- do.call(rbind, lapply(weeks, function(w) {
df_w <- combined_y2 %>%
filter(storage_condition == "CA", Storage.Week == w) %>%
dplyr::select(all_of(vars))
idx <- sample.int(nrow(df_w), size = n0, replace = TRUE)
colMeans(df_w[idx, , drop = FALSE], na.rm = TRUE)
}))
rownames(mat) <- weeks
colnames(mat) <- vars
# sanity checks
dim(mat) # should be 6 36
mat[1:2, 1:6] # preview
is.numeric(mat) # TRUE
# try to run catch22 on one feature to test
f_firm <- catch22_all(mat[, "Firmness"])
f_firm
